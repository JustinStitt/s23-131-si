#include <bits/stdc++.h>

namespace Chess {
  void max(int a, int b) {
    std::cout << "TO THE MAX\n";
  }
}

class Bear {
  private:
    int tall;
    int teeth;
  public:
    Bear(int ta, int te) : tall(ta), teeth(te) {} 
    // initialization list (constructors)
};


int main() {

  std::cout << std::max(5, 2) << "\n";
  justin::max(5, 2); // SRO ::
  // ...
  // int* a = new int(4);
  // std::cout << a << " : " << *a << "\n";
  return 0;
}
